{
  "name": "connect4",
  "version": "1.0.0",
  "description": "Connect 4 web game — spec-driven development POC",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@vitest/coverage-v8": "^1.6.0",
    "serve": "^14.2.3",
    "vitest": "^1.6.0"
  }
}
